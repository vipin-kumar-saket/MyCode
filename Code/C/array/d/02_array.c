#include<stdio.h>
int main()
{
    int i,a[8];
    printf("Enter the elemenet of array :");
    for ( i = 1; i <8 ; i++)
    {
       scanf("%d",&a[i]);
    }
    printf("The size of array is : %d",a[i]);
    return 0;
}